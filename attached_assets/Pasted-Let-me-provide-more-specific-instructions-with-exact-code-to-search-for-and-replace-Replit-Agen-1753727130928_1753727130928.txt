Let me provide more specific instructions with exact code to search for and replace:

## Replit Agent Instructions - EXACT CHANGES

### Change 1: Fix the CSS that hides header
**SEARCH FOR THIS EXACT TEXT:**
```css
        /* Ensure main header is hidden when mobile menu is active */
        body:has(.mobile-menu.active) header,
        body:has(#mobileMenu.active) header {
            display: none;
        }
```

**REPLACE WITH:**
```css
        /* Keep header visible but adjust z-index when mobile menu is active */
        body:has(.mobile-menu.active) header,
        body:has(#mobileMenu.active) header {
            z-index: 40; /* Lower than mobile menu but still visible */
        }
```

### Change 2: Block the interfering script
**SEARCH FOR THIS EXACT TEXT:**
```html
    </style>
    <script>
        // Simple inline tooltip system
        document.addEventListener('DOMContentLoaded', function() {
```

**REPLACE WITH:**
```html
    </style>
    <script>
        // Protect mobile menu elements from external interference
        (function() {
            // Override initializeNavigation if it exists
            if (typeof window.initializeNavigation !== 'undefined') {
                const originalInit = window.initializeNavigation;
                window.initializeNavigation = function() {
                    console.log('Intercepted initializeNavigation call');
                    // Do nothing to prevent DOM destruction
                };
            }
            
            // Set up a watcher for initializeNavigation
            Object.defineProperty(window, 'initializeNavigation', {
                set: function(val) {
                    console.warn('Blocked attempt to set initializeNavigation');
                },
                get: function() {
                    return function() {
                        console.log('initializeNavigation called but blocked');
                    };
                },
                configurable: false
            });
        })();

        // Simple inline tooltip system
        document.addEventListener('DOMContentLoaded', function() {
```

### Change 3: Update toggleMobileMenu function
**SEARCH FOR THIS EXACT TEXT:**
```javascript
        // Mobile menu functionality - Enhanced with close button fix
        window.toggleMobileMenu = function() {
            console.log('toggleMobileMenu called');
            const mobileMenu = document.getElementById('mobileMenu');
            console.log('Mobile menu element:', mobileMenu);

            if (!mobileMenu) {
                console.error('Mobile menu element not found');
                return;
            }

            if (mobileMenu.classList.contains('active')) {
                console.log('Closing mobile menu');
                mobileMenu.classList.remove('active');
                mobileMenu.style.display = 'none';
                document.body.style.overflow = '';
            } else {
                console.log('Opening mobile menu');
                mobileMenu.style.display = 'flex';
                mobileMenu.classList.add('active');
                document.body.style.overflow = 'hidden';
            }
        }
```

**REPLACE WITH:**
```javascript
        // Mobile menu functionality - Enhanced with protection against external interference
        window.toggleMobileMenu = function() {
            console.log('toggleMobileMenu called');
            const mobileMenu = document.getElementById('mobileMenu');
            console.log('Mobile menu element:', mobileMenu);

            if (!mobileMenu) {
                console.error('Mobile menu element not found');
                return;
            }

            if (mobileMenu.classList.contains('active')) {
                console.log('Closing mobile menu');
                mobileMenu.classList.remove('active');
                mobileMenu.style.display = 'none';
                document.body.style.overflow = '';
                // Ensure header remains visible
                const header = document.querySelector('header');
                if (header) header.style.display = '';
            } else {
                console.log('Opening mobile menu');
                mobileMenu.style.display = 'flex';
                mobileMenu.classList.add('active');
                document.body.style.overflow = 'hidden';
            }
        }
```

### Change 4: Remove shared.js script tag completely
**SEARCH FOR AND DELETE THIS ENTIRE LINE:**
```html
<script src="js/shared.js?v=1753723200&cache=false"></script>
```

If the script tag isn't visible in the file, it might be loaded dynamically. In that case, make sure Changes 1-3 are applied correctly.

### Alternative: Nuclear Option
If the above doesn't work, here's a simpler fix - just prevent shared.js from loading at all by adding this at the very top of the `<script>` section (right after the first `<script>` tag):

```javascript
<script>
// Block shared.js from causing issues
window.sharedJsBlocked = true;
Object.defineProperty(window, 'initializeNavigation', {
    value: function() { console.log('Blocked initializeNavigation'); },
    writable: false,
    configurable: false
});
</script>
```

These are EXACT text replacements. The agent should use the search function to find these exact strings and replace them entirely.