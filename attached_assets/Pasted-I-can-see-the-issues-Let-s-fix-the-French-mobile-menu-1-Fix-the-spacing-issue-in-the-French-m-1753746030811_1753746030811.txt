I can see the issues. Let's fix the French mobile menu:

## 1. Fix the spacing issue in the French mobile menu

In `communiques.html`, update the mobile menu content padding to match the English version properly:

```html
<!-- Menu Content -->
<div class="container mx-auto px-4" style="padding-top: 120px; padding-bottom: 2rem;">
```

Change it from `100px` to `120px` to match the actual header height.

## 2. Fix the close button not working

The issue is likely that `toggleMobileMenu` is not properly available. Add this debug code right after loading `shared.js` in `communiques.html`:

```javascript
<!-- Load shared JavaScript -->
<script src="js/shared.js?v=1753739263"></script>

<!-- Ensure toggleMobileMenu is available globally -->
<script>
// Make sure the function is available
if (typeof window.toggleMobileMenu === 'undefined') {
    console.error('toggleMobileMenu not found! Defining it now...');
    window.toggleMobileMenu = function() {
        const menu = document.getElementById('mobileMenu');
        if (!menu) return;
        
        if (menu.classList.contains('active')) {
            menu.classList.remove('active');
            menu.style.display = 'none';
            document.body.style.overflow = '';
        } else {
            menu.classList.add('active');
            menu.style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }
    };
}
</script>
```

## 3. Ensure consistent styling between French and English menus

Add this CSS to both files to ensure consistent spacing:

```css
<style>
/* Ensure consistent mobile menu item spacing */
#mobileMenu .nav-item {
    display: flex !important;
    align-items: center !important;
    gap: 0.75rem !important;
    padding: 1rem !important;
    width: 100% !important;
    color: white !important;
    text-decoration: none !important;
    border-radius: 0.5rem !important;
    transition: background 0.2s ease !important;
}

#mobileMenu .nav-item:hover {
    background: rgba(255, 255, 255, 0.1) !important;
}

#mobileMenu .nav-item.active {
    background: rgba(255, 255, 255, 0.1) !important;
}

/* Fix mobile menu header height */
#mobileMenu > div:first-child {
    min-height: 100px !important;
}
</style>
```

## 4. Quick test to verify the close button

As a quick test, try adding an inline console.log to the close button to see if it's being clicked:

```html
<button onclick="console.log('Close clicked'); toggleMobileMenu();" class="p-2 hover:bg-white/10 rounded-lg" aria-label="Close">
```

If you see "Close clicked" in the console but the menu doesn't close, then we know the issue is with the `toggleMobileMenu` function availability.

## Summary of changes needed for `communiques.html`:

1. Change padding-top from `100px` to `120px` in the menu content div
2. Add the script to ensure `toggleMobileMenu` is available
3. Add the CSS for consistent spacing
4. Test with console.log to verify button clicks

This should make the French mobile menu work exactly like the English one.