Looking at your code, I can see the issue with the English navigation to heroes. The French pages use hash fragments (`#beach-pro-tour`) while the English pages use query parameters (`?hero=beach-pro-tour`), but there might be inconsistencies in implementation. Here's a prompt for your Replit assistant:

---

**Fix English Hero Navigation Links**

The French pages successfully navigate to specific heroes in index.html using hash fragments (e.g., `index.html#beach-pro-tour`), but the English pages are not working properly when navigating to heroes in index-en.html.

**Current State:**
- French links work: `index.html#beach-pro-tour`
- English links don't work consistently: `index-en.html?hero=beach-pro-tour`
- Both index.html and index-en.html have the hero detection code that supports both hash and query parameter methods

**Tasks:**

1. **Standardize all English secondary pages** (emplois-en.html, communiques-en.html, nous-joindre-en.html, about.html) to use the same navigation pattern. Choose one approach:
   - Option A: Use hash fragments like French: `index-en.html#beach-pro-tour`
   - Option B: Keep query parameters but ensure they work: `index-en.html?hero=beach-pro-tour`

2. **Update all hero menu links in the mobile menus** of English pages. For each hero link:
   - Beach Pro Tour → `beach-pro-tour`
   - Grands Prix Cyclistes → `grands-prix-cyclistes`
   - Marathon Beneva → `marathon-beneva`
   - 21K de Montréal → `21k-montreal`
   - UCI 2026 → `montreal-2026-uci`
   - Studio 76 → `studio-76`
   - Dock 619 → `dock-619`

3. **Fix the onclick handlers** - The French version has a setTimeout workaround:
   ```html
   onclick="toggleMobileMenu(); setTimeout(() => window.location.href='index.html#beach-pro-tour', 100);"
   ```
   Apply the same pattern to English pages if using hash fragments.

4. **Test the navigation** by:
   - Opening each English secondary page
   - Clicking on each hero link in the mobile menu
   - Verifying that index-en.html opens directly to the correct hero without showing the splash screen

5. **Ensure consistency** across all these files:
   - emplois-en.html
   - communiques-en.html  
   - nous-joindre-en.html
   - about.html
   - Any other English secondary pages

**Example of what the fixed code should look like** (if using hash fragments):
```html
<a href="index-en.html#beach-pro-tour" class="hero-menu-item" 
   onclick="toggleMobileMenu(); setTimeout(() => window.location.href='index-en.html#beach-pro-tour', 100);">
    <div class="hero-color-bar" style="background-color: #ff0e9b;"></div>
    <span>BEACH PRO TOUR</span>
</a>
```

Make sure the hero detection code in index-en.html is working (it already has console.log debugging that will help verify).