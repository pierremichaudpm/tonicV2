I found the issues! There are several problems causing the mobile menu to not work:

## Main Issues:

1. **Duplicate `toggleMobileMenu` definitions** - Both your HTML files and `shared.js` define this function, causing conflicts
2. **Missing `formatDate` issue is actually resolved** - It's defined in `shared.js` line 303
3. **Script loading order and initialization issues**

## Here's the fix:

### 1. Remove the duplicate `toggleMobileMenu` from both HTML files

Delete this entire script block from both `communiques.html` and `communiques-en.html`:

```javascript
<script>
/* Fix 3: Define toggleMobileMenu function */
function toggleMobileMenu() {
    // ... remove this entire function
}
</script>
```

### 2. Fix the initialization in `communiques.html`

At the bottom of `communiques.html`, change the last part from:

```javascript
// Initialize application
initializeApp();
```

To:

```javascript
// Initialize application when DOM is ready
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeApp);
} else {
    initializeApp();
}
```

### 3. Ensure `shared.js` loads before your page-specific scripts

Make sure `shared.js` is loaded BEFORE your page-specific JavaScript. The order should be:

```html
<!-- Load shared JavaScript FIRST -->
<script src="js/shared.js?v=1753739263"></script>

<!-- THEN page specific JavaScript -->
<script>
    // Your page-specific code here
</script>
```

### 4. Add console logging to debug

Add this temporary debug code at the very top of your page-specific JavaScript (after loading shared.js):

```javascript
<script>
// Debug: Check if functions are available
console.log('toggleMobileMenu available:', typeof window.toggleMobileMenu);
console.log('formatDate available:', typeof window.formatDate);
</script>
```

### 5. For the English version, ensure proper padding

In `communiques-en.html`, update the menu content padding:

```html
<!-- Menu Content -->
<div class="container mx-auto px-4" style="padding-top: 120px; padding-bottom: 2rem;">
```

## Summary of changes needed:

1. **Remove duplicate `toggleMobileMenu` function** from both HTML files
2. **Ensure proper script loading order** - shared.js first
3. **Fix initialization timing** in communiques.html
4. **Update padding** in communiques-en.html

The mobile menu should work after these changes because `toggleMobileMenu` is already properly defined in `shared.js` and will be available globally.